import { LoadButton, LoadWrapper } from "./styled-components";

//This is where the component for the "Load More" button will be build out.

const LoadMore = () => {
	return (
		<LoadWrapper>
			<LoadButton>Load More</LoadButton>
		</LoadWrapper>
	);
};

export default LoadMore;
